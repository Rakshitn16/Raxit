openai>=1.55.0
fastapi>=0.115.0
pydantic>=2.9.0
PyYAML>=6.0.2
apscheduler>=3.10.4
httpx>=0.27.0
python-dotenv>=1.0.1

# Android ships no system timezone database, so Python's zoneinfo has nothing
# to read — every ZoneInfo() call raises ZoneInfoNotFoundError, which takes
# the scheduler down before the server can even start (RoutineRunner builds
# an AsyncIOScheduler(timezone=...) at import). Pure data, no compilation, so
# it costs nothing on the platforms that already have a system database.
tzdata>=2024.1

# Plain uvicorn, not uvicorn[standard]. The extra drags in uvloop, httptools
# and watchfiles, none of which publish a wheel Termux can use — pip falls
# back to building them, and watchfiles wants a Rust toolchain the install
# script does not set up. What the extra actually contributes here is
# `websockets` (below), plus dotenv and PyYAML, which are already direct
# requirements. uvloop and httptools are throughput on a machine serving one
# person, and watchfiles only matters under --reload, which this never uses.
uvicorn>=0.32.0
# Pure Python, and the chat UI's socket does not work without it: uvicorn with
# no websocket implementation rejects the upgrade outright.
websockets>=13.0
